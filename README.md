# STM32_NUCLEO_C031C6_Blinky_Baremetal
Minimal C implementation for LED blink on STM32C031C6. Uses CubeMX only for SWD/debug and clock initialization. All application logic (GPIO configuration and toggling) is done via direct memory-mapped register access.
